/**
 * Number of nonce records that fits into each send bucket _(1 byte)_.
 *
 * Related contracts:
 * - {@link !FlexSendStateBucket | `FlexSendStateBucket`}
 *
 * @category Constants • Send
 */
export const FLEX_SEND_NONCES_PER_BUCKET = 96n;
